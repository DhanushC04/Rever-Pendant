{
  "name": "Email & Reminder System Tests",
  "description": "Test requests for debugging email/reminder functionality",
  "client": "REST Client",
  "variables": {
    "baseUrl": "http://localhost:5000",
    "userId": "1",
    "keynoteId": "1",
    "reminderId": "1"
  },
  "requests": [
    {
      "name": "1. Get API Health",
      "method": "GET",
      "url": "{{baseUrl}}/api/health",
      "description": "Check if backend is running"
    },
    {
      "name": "2. Get All Conversations",
      "method": "GET",
      "url": "{{baseUrl}}/api/conversations",
      "description": "Get list of conversations (find keynote_id)"
    },
    {
      "name": "3. Get Conversation Details",
      "method": "GET",
      "url": "{{baseUrl}}/api/conversations/1",
      "description": "Get conversation with keynotes (find keynote_id from here)"
    },
    {
      "name": "4. Debug - Check Reminders Status",
      "method": "GET",
      "url": "{{baseUrl}}/api/debug/reminders",
      "description": "🔍 MOST IMPORTANT - Shows scheduled jobs & database records. Use this to verify setup!"
    },
    {
      "name": "5. Create Reminder - 2 minutes from now",
      "method": "POST",
      "url": "{{baseUrl}}/api/reminders",
      "headers": {
        "Content-Type": "application/json"
      },
      "body": {
        "raw": "{\"user_id\": 1, \"keynote_id\": 1, \"reminder_time\": \"2024-12-02T15:35:00\"}"
      },
      "description": "⏰ Creates a test reminder. Replace reminder_time with a future time (+ 2 min to test).",
      "note": "Example times:\n- 2 minutes later: 2024-12-02T15:35:00\n- 5 minutes later: 2024-12-02T15:38:00\n- Full format: YYYY-MM-DDTHH:MM:SS"
    },
    {
      "name": "6. Check Reminder After Creation",
      "method": "GET",
      "url": "{{baseUrl}}/api/debug/reminders",
      "description": "Check if reminder is in scheduled_jobs list"
    },
    {
      "name": "7. Get User Reminders",
      "method": "GET",
      "url": "{{baseUrl}}/api/reminders/user/1",
      "description": "Get all reminders for user 1 (check is_sent flag)"
    },
    {
      "name": "8. Delete Reminder",
      "method": "DELETE",
      "url": "{{baseUrl}}/api/reminders/{{reminderId}}",
      "description": "Cancel a scheduled reminder"
    },
    {
      "name": "9. Get System Status",
      "method": "GET",
      "url": "{{baseUrl}}/api/status",
      "description": "Get full system status with all logs"
    }
  ],
  "workflow": [
    {
      "name": "Quick Test Workflow",
      "description": "Complete workflow to test email reminders",
      "steps": [
        "1. GET /api/health - Verify backend running",
        "2. GET /api/conversations - Find a keynote_id (or create one via start process)",
        "3. GET /api/debug/reminders - Check initial state",
        "4. POST /api/reminders - Create reminder with time = 2 minutes from now",
        "5. GET /api/debug/reminders - Verify reminder in scheduled_jobs",
        "6. WAIT 2 minutes (watch console for logs starting with ⏰)",
        "7. Check email inbox - You should have received reminder!",
        "8. GET /api/debug/reminders - Check if reminder shows is_sent: true",
        "9. GET /api/reminders/user/1 - Verify sent_at timestamp"
      ]
    }
  ],
  "console_watch_for": {
    "success": [
      "✅ [CREATE REMINDER] Job scheduled successfully",
      "✅ [REMINDER EMAIL] SUCCESS - Reminder X sent",
      "⏰ [REMINDER EMAIL] Database connection closed"
    ],
    "errors": [
      "❌ [REMINDER EMAIL] ERROR:",
      "❌ [KEYNOTE EMAIL] FAILED",
      "❌ [CREATE REMINDER] FAILED"
    ]
  }
}
